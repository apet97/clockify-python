"""Console entry point for the Clockify MCP server."""

import sys


def main() -> int:
    print("clockify-mcp: server not built yet (Phase 6 pending)", file=sys.stderr)
    return 1


if __name__ == "__main__":
    sys.exit(main())
